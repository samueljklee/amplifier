[project]
name = "scenario-generator"
description = "Generate amplifier scenarios from requirements using Claude Code SDK"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
dev-dependencies = []
